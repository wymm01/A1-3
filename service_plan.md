# 🍸 AI Mixologist 서비스 기획서

## 1. 서비스 개요

### 서비스명
AI Mixologist

### 한 줄 소개
사용자의 기분과 취향을 입력받아 AI가 어울리는 음료를 추천해주는 웹 서비스

---

## 2. 서비스 목적

AI Mixologist는 사용자가 현재 자신의 기분과 선호하는 재료 또는 취향을 간단하게 입력하면, AI가 해당 상황에 어울리는 음료를 추천해주는 서비스이다.

어떤 음료를 선택할지 고민하는 사용자가 복잡한 검색 과정 없이 자신의 상황에 맞는 음료와 레시피를 빠르게 확인할 수 있도록 하는 것을 목적으로 한다.

또한 생성형 AI API와 웹 프론트엔드를 연결하여 실제 사용자가 이용할 수 있는 AI 웹 서비스를 구현하는 것을 목표로 한다.

---

## 3. 타겟 사용자

- 현재 기분에 어울리는 음료를 찾고 싶은 사용자
- 어떤 음료를 선택할지 고민하는 사용자
- 새로운 음료 레시피를 찾는 사용자
- 자신의 취향을 반영한 음료 추천을 받고 싶은 사용자
- 생성형 AI를 활용한 추천 서비스를 경험하고 싶은 사용자

---

## 4. 서비스 페이지 및 섹션 구성

### ① 서비스 소개

AI Mixologist가 어떤 서비스인지 소개하고 주요 기능과 이용 방법을 안내한다.

### ② AI 추천 받기

사용자가 현재 기분과 선호 재료 또는 취향을 입력한다.

입력된 정보는 백엔드 API를 통해 Gemini AI에 전달되며, AI가 사용자의 상황에 적합한 음료를 추천한다.

### ③ FAQ

서비스 이용 방법과 AI 추천 기능에 대한 자주 묻는 질문을 제공한다.

---

## 5. 핵심 기능

### 5-1. 기분 입력

사용자가 현재 자신의 기분을 입력할 수 있다.

예:
- 피곤함
- 신남
- 우울함
- 스트레스
- 상쾌함

### 5-2. 취향 입력

사용자가 원하는 재료나 음료 스타일을 입력할 수 있다.

예:
- 상큼한 과일
- 탄산
- 차
- 달콤한 맛

### 5-3. AI 맞춤 음료 추천

사용자가 입력한 기분과 취향을 Gemini AI에 전달하여 맞춤형 음료를 추천받는다.

### 5-4. 추천 결과 출력

AI가 생성한 결과를 웹 화면에 표시한다.

추천 결과에는 다음과 같은 정보가 포함된다.

- 추천 음료
- 추천 이유
- 음료 레시피

### 5-5. 오류 및 예외 처리

사용자의 입력 누락이나 AI API 오류가 발생했을 때 사용자가 상황을 알 수 있도록 안내한다.

---

## 6. AI 기능 설계

### 6-1. 입력

AI 추천에 사용되는 입력값은 다음과 같다.

1. 현재 기분
2. 선호 재료 또는 취향

예:

```text
기분: 피곤함
취향: 상큼함, 탄산
```

### 6-2. AI 처리

프론트엔드 JavaScript에서 사용자의 입력을 받아 /api/recommend 엔드포인트로 POST 요청을 보낸다.

Vercel Serverless Function으로 실행되는 Python 백엔드가 입력값을 받아 Gemini API에 전달한다.

Gemini AI는 사용자의 기분과 취향을 바탕으로 적합한 음료를 생성한다.

### 6-3. 출력

AI가 생성한 추천 결과를 사용자 화면에 표시한다.

주요 출력 내용:

음료 이름
추천 이유
추천 레시피

---

## 7. AI 프롬프트 설계

AI에게 전문 바텐더 및 음료 소믈리에 역할을 부여하고 사용자의 기분과 선호 재료를 전달하여 적절한 음료를 추천하도록 설계하였다.

프롬프트에는 다음 정보가 포함된다.

전문 바텐더 및 음료 소믈리에 역할
사용자의 현재 기분
사용자의 선호 재료 및 취향
음료 1가지 추천
추천 이유
구체적인 레시피

---

## 8. 실패 처리 기준

### 8-1. 빈 입력

현재 기분은 필수 입력값으로 설정한다.

사용자가 기분을 입력하지 않고 추천을 요청하면 다음과 같은 안내를 제공한다.

기분을 입력해주세요.

### 8-2. AI API 오류

Gemini API 호출 과정에서 오류가 발생하면 프론트엔드에서 오류를 처리한다.

사용자가 서비스 이용을 계속할 수 있도록 기본적인 음료 추천 결과를 제공하는 fallback 기능을 적용하였다.

### 8-3. 서버 오류

백엔드에서 문제가 발생하면 JSON 형태의 오류 메시지를 반환한다.

이를 통해 프론트엔드에서 API 오류 상황을 확인하고 사용자에게 안내할 수 있도록 구성하였다.

---

## 9. 사용자 동작 흐름

사용자
  ↓
현재 기분 입력
  ↓
선호 재료/취향 입력
  ↓
추천받기 버튼 클릭
  ↓
JavaScript fetch()
  ↓
/api/recommend 호출
  ↓
Python Serverless Function
  ↓
Gemini API 호출
  ↓
AI 추천 결과 생성
  ↓
JSON 응답
  ↓
JavaScript가 결과를 화면에 표시

---

### 10. 기술 구성

Frontend
HTML
CSS
JavaScript
Backend
Python
Vercel Serverless Functions
AI
Google Gemini API
google-genai SDK
Deployment
GitHub
Vercel

---

### 11. 반응형 설계

데스크톱, 태블릿, 모바일 환경에서도 서비스를 사용할 수 있도록 반응형 레이아웃을 적용한다.

Chrome 개발자 도구의 모바일 기기 에뮬레이션을 이용하여 모바일 화면에서 다음 기능을 확인하였다.

메뉴 표시
입력창 표시
추천 버튼 표시
AI 추천 결과 표시
화면 폭에 따른 레이아웃 변화

---

### 12. 보안 및 환경 변수

Gemini API 키는 소스 코드에 직접 작성하지 않고 환경 변수로 관리한다.

Vercel 환경 변수에 다음 키를 설정한다.

GEMINI_API_KEY

API 키는 다음 장소에 직접 노출하지 않는다.

소스 코드
GitHub 저장소
README.md
스크린샷
공개 문서

API 키가 외부에 노출되었을 경우 기존 키를 폐기하고 새로운 API 키를 발급한다.

---

### 13. 기대 효과

사용자는 자신의 기분과 취향을 간단하게 입력하는 것만으로 새로운 음료를 추천받을 수 있다.

이를 통해 단순한 음료 검색보다 개인의 상황과 취향을 반영한 추천 경험을 제공할 수 있다.

또한 사용자의 입력부터 AI 처리, 결과 출력까지 연결된 실제 AI 웹 서비스의 전체 구조를 경험할 수 있다.

---

### 14. 향후 개선 계획

향후 다음과 같은 기능을 추가할 수 있다.

추천 음료 이미지 제공
음료 추천 기록 저장
즐겨찾기 기능
사용자별 추천 이력 관리
다크 모드
방문자 분석
추천 결과를 외부 저장소에 저장
음료 종류별 상세 검색 기능

---

### 15. 프로젝트 목표

본 프로젝트를 통해 다음 내용을 직접 구현하고 이해하는 것을 목표로 한다.

HTML/CSS/JavaScript의 역할 이해
JavaScript의 fetch를 이용한 API 요청 이해
Python Serverless Function의 동작 구조 이해
Gemini AI API 연동
환경 변수를 이용한 API 키 관리
GitHub를 이용한 코드 관리
Vercel을 이용한 웹 서비스 배포
AI 코딩 도구를 활용한 개발 및 오류 수정
반응형 웹 서비스 구현